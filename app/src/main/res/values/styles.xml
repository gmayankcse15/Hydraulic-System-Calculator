<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="headerTextStyle">
        <item name="android:textColor">@color/blue</item>
    </style>

    <style name="cylinderTextStyle">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/cylinder_textsize</item>
    </style>

    <style name="cylinderButtonStyle">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@drawable/button_drawable_grey</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">@dimen/button_text_size</item>
    </style>

    <style name="editBoxStyle">
        <item name="android:background">@drawable/edittext_box_style</item>
        <item name="android:maxLines">1</item>
        <item name="android:textAlignment">viewEnd</item>
        <item name="android:inputType">numberDecimal</item>
        <item name="android:textSize">16dp</item>
    </style>

    <style name="customSpinnerStyle" parent="@android:style/Widget.ListView.DropDown">
        <item name="android:background">@drawable/spinner_drawable</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="navigationViewStyle">
        <item name="android:textSize">@dimen/navigation_textSize</item>
    </style>

    <style name="tagline_textStyle">
        <item name="android:textSize">@dimen/taglineTextSize</item>
        <item name="android:textColor">@color/blue</item>
    </style>

</resources>
